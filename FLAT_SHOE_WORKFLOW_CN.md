# 复杂网格平底鞋：空间权重场与间距对照

本流程适用于已完成静态适配、拓扑索引对应且鞋带骨链已接入目标骨架的平底鞋。高跟鞋的目标姿势适配另行处理。实现入口为 `shoe_workflow.run`，数值部分为 `shoe_field`；不调用旧的手工权重传递或 Robust 算法。

## 最终流程

1. 核对源鞋、适配鞋、目标身体、原始完整权重、绑定和拓扑；保存配置与原权重快照。只输出独立 A/B 副本。
2. 显式审核原身体骨与鞋带动骨。保留每个顶点左右腿身体份额、各根动骨的归一化份额。原始总权重不为 1 时按完整有效影响归一化。未知骨、缺失绑定、重叠分类、无权重顶点中止。
3. 当前鞋子采用目标空间分布策略；这不是原衣物动作回归。需显式记录鞋带审核决定，不以数值分类直接授权。原始微小身体份额也保留，纯动骨为零的身体预算不补全。
4. 候选身体骨必须在目标身体实际存在正权重；运动控制骨不自动成为写入骨。例如本例 `cf_j_leg03` 没有身体权重，使用实际小腿变形组。
5. 在临时静止身体与临时查询鞋上调用 Blender Data Transfer：`VGROUP_WEIGHTS / POLYINTERP_NEAREST / REPLACE`。不在屈曲身体上采样静止鞋子。
6. 长鞋头：从同侧身体权重覆盖几何计算前端边界，将超出边界的查询点沿显式前掌方向拉回有效区域，再使用相同原生传递。只改变临时查询点，不移动最终鞋子顶点。前掌权重由此向鞋尖延伸，距离变远不等于身体份额应变零。当前自动延伸仅支持前端；其他缺少可信采样的部位中止，需显式补充区域策略。
7. 对同侧非零身体预算顶点的条件身体比例做图平滑。图只来自实际拓扑边，不做跨层、跨左右脚的空间 KNN。采样可信度由距离、邻域离群程度决定，各独立分片保留数据参照。候选范围为原生采样非零组及两环邻域，防止求解器引入未在鞋部采样到的膝部骨。
8. 乘回原身体预算，鞋带各骨份额原样恢复，生成 **A.Field**。不在结果上执行全组归一化或旧的动骨擦除。
9. 用目标脚踝、前掌和侧倾动作采样，优化 **B.Gap** 候选。目标为保持已适配鞋与目标身体的间距，而非复现原鞋运动。身体参照采用静止身体三角面及重心坐标；法向偏移为主、弱切向项防滑移、先验项防过改。远端参照降低置信度。原动骨和左右身体预算冻结。
10. 用未参与训练的姿态，检查真实最近表面距离变化、鞋子三角面与身体交叉、顶点/边中点/面中心侵入。B 必须收敛、间距 RMS 不劣于 A，且每个验证姿态的交叉数、侵入数与最大深度不恶化，才写出 B；失败保留 A 和诊断，禁止手改 accepted。
11. 用 Blender 实际求值检查线性蒙皮计算及浮点写回一致性，验证原对象/身体/姿态恢复。用户再比较 A/B 外观与动作。

## 求解器与边界

连续性修复使用保单纯形的 Jacobi 迭代；间距代理目标是固定参照下的凸二次问题，采用带安全步长的投影梯度迭代。仅依赖 Blender 自带 NumPy；没有增加 SciPy/OSQP 安装前提。记录收敛残差，未收敛不自动接受。

真实最近表面距离是独立验收量，不声称凸代理目标等同于精确非线性距离最小化。穿模目前是验收门槛，不是主动碰撞修复器。如果 A、B 都已有穿模，非恶化不表示无穿模，报告中的绝对计数仍需处理。

固定离散姿态不保证所有动作和连续时间无穿模；不检查鞋子自碰撞、其他衣物碰撞或物理模拟。纯动骨区域不作为身体权重优化与穿模验收目标。不同断开分片之间不自动创建关联，需要明确关系后才能增加关联约束。

## 调用

```python
from kk_vrc_cloth_tools import shoe_workflow
report = shoe_workflow.run(source_shoes, fitted_shoes, target_body,
                          reviewed_config, round_directory)
```

侧栏“复杂网格平底鞋”提供完整配置面板，日常操作不需要 JSON。仍需审核骨骼与方向，不能自动识别所有鞋子的设计意图。JSON 导入/导出作为可选的复用入口保留。

## 面板操作（0.2.6）

1. **输入与扫描**：选择原鞋（完整原权重）、适配鞋、目标素体，点击扫描。三个对象独立；原鞋与适配鞋拓扑一致，适配鞋与身体共用目标骨架。
2. **审核身体与鞋带区域**：已知源身体骨自动建议角色；未知骨保留待判断。鞋带按第一节根骨链列出，选择左右侧后可将整条根链确认为动骨。用“查看网格范围”显示该链在适配鞋上的实际源权重范围。确认均匀、边缘渐变或纯动骨模式；自动无法判断的区域必须逐顶点分析、填写结论，然后勾选已核对。手动修改身体骨角色后点击刷新区域。所有源组必须有明确角色和侧别。
3. 展开目标候选列表，只会列出当前身体实际有正权重且可绑定的骨骼；勾选同侧脚部候选。确认“使用目标脚空间分布，保留原身体/动骨份额”。该复选框不代表自动批准所有未知模式。
4. **方向、延伸与连续性**：指定左右脚的脚踝、前掌动作骨。可从关节位置推断前掌方向，或直接填写世界方向及上方向；不得按骨轴朝向猜测。设置长鞋头延伸开关、平滑强度、候选拓扑环数和最低可信采样量。
5. **姿态与优化**：使用 7 个标准训练、4 个独立验证姿态；配置随机辅助数量、权重和种子，面板显示有效权重。可关闭一键流程中的间距优化，调整保留 A 的约束强度。
6. **执行**：先选择结果目录。检查配置后，可以先“生成 A：采样与连续性”，再“从 A 优化 B”；也可一键运行。A 单独生成时尚未进行动作/碰撞验收，B 阶段或完整流程才会执行。分阶段 B 复用已保存的 A 上下文，不重新传递权重。
7. 源鞋、适配鞋、身体、A 权重或姿态变更，或者采样/区域/连续性设置变更时，拒绝复用过期 A；须重新生成 A。姿态预设和随机参数、优化先验可以调整后从 A 重新优化。
8. **结果**：显示 A/B 名称、数值状态与间距 RMS，按钮切换适配原件/A/B，或打开完整报告目录。B 未通过时保留 A。结果对象需要保存 blend 才持久保存。

配置与结果指针保存为场景属性，随 blend 保存。扫描、配置导入不会自动勾选新区域的审核；旧案例设置可以由 Agent 在已获得用户验收的前提下恢复。旧结果若无可恢复的 A 上下文，可以切换查看但不能从中继续 B。

每轮目录保存 `config.json`、`before.json`、`initial.npz`、`candidate.npz`、`report.json` 和进度。函数不自动保存 blend，调用者须明确保存测试项目副本。

设置 `optimize_gap: false` 可只生成和验收 A，不生成 B；默认 true。`candidate_rings` 默认 2，`smoothing` 默认 0.7，`prior` 默认 0.1。参考配置见 `examples/flat_shoe_neon_vertex.json`，其中对象名、源骨角色和世界方向必须按当前模型核对。

## 本例配置

### 平底鞋姿态预设（0.2.5）

完整面板提供 **平底鞋标准 + 随机辅助** 的参数。旧的高级配置执行入口仍可保留文件中的自定义动作。预设由 `shoe_presets.py` 生成，不写死骨名；以每侧 `foot`、`toes` 运动控制骨和已审核的世界 `up/forward` 方向构造旋转轴。

- 7 个标准训练动作权重均为 1：踝 +20°、−20°；镜像侧倾 ±10°；前掌 +18°、−12°；踝 +12° 加前掌 +10°。
- 4 个独立验证动作保持原样：静止；踝 +13°/前掌 +7°；踝 −14°/前掌 −8°；踝 +5°/前掌 +11°/镜像侧倾 7°。不参与求解。
- 默认增加 6 个随机训练动作，每个权重 0.15，种子 20260919。每侧独立采样：踝 −15°～+15°，前掌 −8°～+12°，侧倾 −6°～+6°。当组合幅度超出单位椭球时向中立姿态收缩，避免各关节同时落在极端角点。
- 随机样本总权重上限为标准训练总权重的 20%；数量过多时降低每样本实际权重。默认总权重 0.9，标准总权重 7。
- 可关闭随机、修改数量/种子/相对权重；范围固定在 V1 保守预设中。自定义动作仍可使用配置文件模式。
- 每轮保存生成后的完整姿态与 `pose_preset_resolved`，包括实际权重、种子和范围，方便复现。增加随机训练不会替换独立验收，验收门槛也不放宽。

API 配置示例：

```json
"pose_preset": {
  "id": "FLAT_SHOE_V1",
  "random_enabled": true,
  "random_count": 6,
  "random_weight": 0.15,
  "seed": 20260919
}
```

没有 `pose_preset` 时继续使用原 `poses` 列表；有预设时生成本轮有效姿态，但不原地修改传入配置或覆盖历史测试报告。

当前 `Shoes` → `Shoes.001`，身体 `o_body_a_0`。左右 B2/B3 为边缘渐变；B1 自动评分略低于阈值，人工审核其连接端衰减；B4 主链渐变，独立末端存在极小身体残留，保留原值不清零。本轮使用显式鞋类空间策略，不能反向宣布通用衣物自动分类器已经支持所有复杂混合模式。

实测配置和 A/B 报告保存在 Neon Vertex 测试目录的 `shoe-transfer/flat-round-02`，不将个人模型文件放进插件仓库。
