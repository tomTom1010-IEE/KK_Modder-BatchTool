# 饰品独立骨架预处理（0.2.19）

侧栏独立标签 **model preprocess**。每次只处理一个源饰品，并生成独立网格、Armature 对象和独立骨架数据。即使多个饰品原来共用身体骨架，也不合并结果骨架。衣物模式仍保护标准身体骨；饰品模式在明确替代挂点后移除身体骨，两种策略不混用。

## 使用

1. Object 模式选择源饰品，使用活动网格。选择输出目录。
2. 指定已经确认的“自有链根”，包含其完整后代和必要依赖；非标准骨不会自动认定为自有动骨。可在源骨架 Pose 模式选骨后点加入。
3. “外部挂点骨”可填写人物身体骨、其他衣物的骨、头发骨等。MMD 标准身体骨可按开关自动识别；VRC/其他命名可关闭该开关，提供明确的外部骨清单。返回 Object 模式扫描。
4. 查看计划：源骨到 `ACC_Attach_*` 的映射、保留链、删除清单。单挂点对应一个固定代理，多挂点分别保留，另有组织用的 `ACC_Root`。第一根动骨不接收原固定部分的权重。
5. 执行后先保存完整备份，再构造独立副本并进行权重、静止和动作对比。失败删除本次未完成的副本，不修改源件。每轮保存 before.blend、result.blend、result.json 和 weights.json。

## 保证与边界

- 外部骨权重逐值改到对应固定代理；自有骨每个顶点的权重不变。不使用最近面插值，不删除后归一化。MMD 元数据组保留。
- 自有末端、中间骨、MMD 追加变换及必要 dummy/shadow 通过依赖闭包保留；内部父级、约束引用在重命名后继续指向当前独立骨架。
- 检查有效骨权重规范化；不支持的 SDEF/形态键、DQS、动画/驱动、额外修改器或外部/自定义空间约束会停止，不清空功能绕过检查。
- 验证拓扑、UV、材质、锐边、自定义法线、静止外观，以及挂点、自有骨和两者同时运动的实际 Blender 求值。
- 源与结果各用独立的测试骨架，测试不修改源角色姿态。
- 输出状态为 `PENDING_TARGET_ATTACHMENT`：固定代理保存当前姿态和源挂点元数据，但**没有连接目标角色，也没有外部约束依赖源角色**。动作验证时由测试代码同步挂点矩阵；目标接入时必须落实这些映射，不能把验证通过理解为已经自动绑定目标。
- 多挂点代理不能无损合并成一个刚性骨。例如领带的胸/颈、绳子的两根裙摆骨、手套各指节，默认保留各自代理。它们仍在同一件饰品自己的独立骨架内，不与其他饰品共用骨架。
- 保留骨链不等于完成物理运行时迁移。本版本不复制或重建 MMD 刚体、关节及目标游戏物理参数。
- 每件衣物也必须有独立骨架；不要为统一预处理而把最终裙子、腿等独立物品留在共享骨架上。

## Agent 接口

```python
from kk_vrc_cloth_tools import accessory_preprocess as api
cfg = {"mmd_body": True, "owned_roots": [...], "external": [...]}
plan = api.scan(source, cfg)
result = api.apply(source, cfg, plan["stamp"], output_directory)
```

逐件扫描和处理，不能将全场景名字相似的骨直接批量归为某饰品。已处理对象禁止再次处理；源数据或 UI 配置变更后重新扫描。失败时保留其他成功资产，修正失败资产的具体归属或能力缺口后再执行。
