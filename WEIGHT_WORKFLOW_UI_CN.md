# 衣物权重工作流 UI（0.2.3）

## 新手保守预设（独立面板）

在 KK/VRC Tools 顶部的“衣物权重 · 新手保守预设”中选择原衣物、已适配衣物和目标素体，设置结果目录，然后点击“① 应用保守预设并自动扫描”。已保存的 blend 可默认使用同目录的 `KKVRC-Weight-Results`；未保存文件需手动选目录。外部 Python 仍需 NumPy/SciPy/OSQP，可在面板下方检查环境，不自动下载依赖。

应用前备份旧配置 JSON，保留已有骨骼映射、区域验收和末端选区；数值参数和动作列表按所选预设重新生成。动作范围可选自动/上身/下身/全身，自动模式依据已识别的身体骨语义。默认保留原衣物已有手指份额（本例只有拇指）；采样不会引入其他手指。找不到指节或同侧手掌映射时停止，不丢弃份额。

`conservative_v1` 沿用已测试流程参数：原生最近面插值、身体采样可信度 1、开启穿模约束；主体 prior/smooth 均为 0.002、trust 0.15、最多 4 次接触迭代。常用动作权重大于固定种子 31415 的随机组合，并保留独立验证动作。这是保守起点，不是对所有模型的成功保证。

扫描会粗选区域模式，但仍须逐区高亮核对、确认动骨身份和模式；无法识别的区域需要 Agent 或人工分析。高亮进入编辑模式后按 Tab 回物体模式。动作预览/恢复以及复杂映射可在详细面板中处理。预设参数或动作被改动后，重新套用预设，或用详细流程执行自定义设置。

确认后点击“③ 自动计算 → 验收 → 生成副本”：前置检查 → 六类初值副本 → 主体求解 → 独立验证 → 通过后写入主体副本。默认只做主体；可勾选接着执行末端精调，但须先在详细流程指定局部顶点与允许骨骼，然后重新应用预设。末端使用 T pose 手腕动作、过渡 2 环、小角度 8 度，prior 0.00005、smooth 0.00003、residual_scale 0.15、trust 0.35、最多 8 次接触迭代；核心修正上限 1，邻圈 0.25、0.06。不会自动扩大局部范围或添加不在素体权重白名单中的骨骼。

求解中按 Esc 可取消。未知模式、未确认区域、失效配置、缺少局部范围或独立验收失败会停止；已生成的初值或主体副本与日志保留，原对象和权重保留。不自动保存 blend。新手按钮调用与详细模式相同的规划、求解、验证和写入接口。

## 0.2.2：统计粗识别与逐区验收

扫描根骨区域后会预选三类建议：均匀跟随、边缘渐变、无法识别。也可点击“统计粗识别”补做；已有且未过期的验收保持不变。人工改选的未验收模式保留，统计建议另行显示。

用完整原权重计算 `身体 / (身体 + 保留动骨)`，查看均值、方差、极差、相对极差、P05/P95；均匀模式还检查各身体骨内部占比的波动。渐变模式结合原网格边邻接、身体主导连接边缘、拓扑距离、逆向波动比例和单调拟合程度。采用单调递减拟合以支持“窄连接带 + 长零权重自由端”，并非要求线性衰减。不会按骨名或服装部件名直接定模式。

相互断开的网格片分别判断；结论冲突、样本不足、未知骨权重、缺少连接边缘、稳定跟随底座叠加渐变等情况返回无法识别。纯动骨分片不投票，零身体预算保持不变；整区纯动骨没有身体跟随模式可识别。阈值是保守启发式（`region_patterns.py`），不是经训练校准的识别准确率。

操作：逐项高亮查看 → 接受建议或人工改选模式 → 点击“确认本区域模式”。无法识别的区域不能直接确认，需 Agent 逐顶点分析或人工处理后再决定适用模式；不能仅为通过检查随意改选。候选骨的 DYNAMIC 身份仍须确认，统计分析不会修改源角色或权重。

未逐区确认不能运行迁移。原权重、拓扑、角色、根骨或范围变化会使确认过期，模式改选也需重新确认。配置导入/升级后的旧区域没有确认记录时需要重新验收。

## 根骨扫描与动作预设

无自身权重、汇集多条受权重影响支链的组织根会拆成各支链候选，例如跳过 `Jacket_Root`，使用 `Jacket_B1_L` 等第一节。明确标记为 DYNAMIC 的根保留整体；拓扑推断不能证明物理语义，候选根可手动调整。

“扫描骨骼”现在会遍历完整源骨架，自动生成以第一节为入口的待确认区域；第一节没有直接权重、但后续骨有权重时也能识别。已知身体骨和已标明 BODY/IGNORE 的骨是边界。未知支链只是候选，用户仍需确认它是衣物动骨，并选择均匀/渐变模式。“按第一节动骨补充区域”只补缺失根骨，保留已有模式和显式选区。

第三步现在有“上身 / 下身 / 全身 / 手腕末端”预设和“生成预设动作（补充缺项）”。预设根据源骨动作语义及目标动作映射生成，不要求逐条手填坐标；缺少映射会报告跳过项，无法确定解剖方向时会停止生成，不会静默套用世界 XYZ。非已识别命名可在源骨列表设置动作语义。

方向由静止姿态的左右肢体及躯干推导，源和目标分别计算；手腕扭转沿肘到腕方向。上身包括抬臂、前后摆臂、屈肘和躯干三轴；下身包括抬腿、外展、屈膝和躯干；全身合并两者。腕部包含左右扭转、弯曲和侧偏，扭转采样 ±8/30/50 度，其他默认 ±8/30 度，可调整。预设只建立可编辑的动作方案，不保证任意模型的所有方向和幅度均合适，生成后用“预览动作 / 恢复姿态”核对。

高级动作设置支持目标骨列表及旋转分摊比例（正数、总和为 1）。确认目标采用 cf_j_spine02 → cf_j_spine03 父链时，源胸部预设使用两骨各 0.5；其他骨架按已配置单骨映射生成，可自行编辑分摊。动作骨不等于接收权重的骨，身体权重白名单继续独立检查。重复生成不会覆盖已有预设或手动编辑，若要按新映射重新生成某项，应先删除该项。

KK/VRC Tools 中原服装权重步骤替换为四阶段面板。骨架嫁接、手套姿态对齐、空组清理和拓扑导出仍在原工具区；旧的动骨区域身体权重擦除不再展示为流程步骤。嫁接默认保留源骨架，因为运动响应优化需要完整源权重与骨架。

1. **输入与骨骼检查**：指定完整源衣物、已适配衣物、目标素体。扫描后核对每个有效源骨的角色、六类归属、接收权重的目标骨、对应动作骨和手指策略。已知 VRC 身体结构作初始建议，未知骨保持 REVIEW。目标列表来自素体真实正权重，归属不明项必须确认。
2. **动骨区域的身体跟随模式**：添加区域，选源骨链根；可将审核过的整条衣物链标为 DYNAMIC。区域范围从该链的源非零权重生成，也可高亮后修正选区并记录。显式选区覆盖骨链范围，允许一链拆成多个模式。UNIFORM 使用语义初值，EDGE_GRADIENT 允许可信原生采样；不改六类预算。未确认、复杂混合、模式冲突或混合顶点缺少覆盖会阻止执行。
3. **六类迁移与主体优化**：选择初值来源、结果目录，预览并写入独立初值副本。空间采样固定为 Blender 原生最近面插值；原生采样要求共同静止姿态。配置源/目标动作骨、世界轴与幅度，常用动作双向采样，额外生成固定种子低权重随机组合和不同角度独立验证。点击求解后再独立验收，通过后写入主体副本。
4. **末端精调**：在对应网格记录末端核心顶点，在目标骨列表勾选同侧允许调整的候选，在动作列表标记末端动作。身体保持 T pose，只采样这些末端骨；拓扑过渡环与修正幅度受限。独立验收通过后写入末端副本。

模式配置实际进入 `prepare_plan/prepare_from_body(region_modes=...)`，不是旧 `reviewed_patterns` 字符串。模式控制初值来源；不会把整片衣物强制设成相同权重。优化阶段仍遵守逐顶点六类预算、固定动骨及启用手指，并拟合完整源响应。

“接收权重的目标骨”与“对应动作骨”可能不同：目标父控制骨可以参与采样动作，但衣物身体权重只写给素体实际已有正权重的骨。对关闭的手指，明确选择同侧非手指接收骨；同指各指节开关一致。开启手指则匹配同指指节并保留原份额。

运行设置：外部 Python 需安装 requirements-optimizer.txt 中的 NumPy/SciPy/OSQP，可填写依赖目录；面板有依赖检查，不自动下载。求解为隐藏外部进程，Esc 取消后不会写入模型。导出场景及 Blender 独立几何检查仍在 Blender 主线程运行，大模型时可能暂时忙碌。

所有结果写入独立副本，旧衣物保留。没有自动保存 blend。配置可导出/导入 JSON，运行目录包含初值计划、上下文、候选、日志、独立验证和写入记录。配置变化会使旧阶段候选失效；新增末端选区不要求重新生成主体初值。

使用者必须先确认顶点索引语义对应、骨骼映射和动作轴合理。测试通过不代表自动理解任意骨架；不支持的驱动/约束、DQS、多修改器与复杂身体跟随模式会拒绝执行。穿模检查不覆盖连续时间、衣物自碰撞或其他叠穿衣物。
