# MMD 衣物预处理（0.2.18）

在 3D View 的 N 侧栏新增独立的 **model preprocess** 标签，不向 KK/VRC Tools 原标签追加面板。规则和骨骼映射子面板默认折叠。

用于把单件衣物及其必要骨架复制为可编辑的 T pose 基线。原模型保持原状；不传递权重、不适配目标体型、不清空必要约束、不迁移运行时物理。

## 使用步骤

1. 在 Object 模式选择原始衣物，点击“使用活动网格”。指定备份/结果目录；默认 `//MMD-Preprocess` 位于当前项目文件旁。未保存项目时必须指定绝对目录。
2. 点击“扫描清理预览”。展开“保留规则与清理预览”，核对保留/候选删除列表。扫描以当前这件衣物为范围，其他使用同一骨架的网格只记录为共享使用者，原共享骨架不删改。
3. 配置有歧义的骨骼归属：
   - **额外保留骨**：保持指定节点及必要依赖；包括需要的解剖参考。
   - **完整保留链根**：包括其后代。若包含其他衣物的支链，应在下项排除。
   - **确认清理分支根**（接口字段 `excluded`）：明确确认整条分支无须保留后使用；受保护身体骨、显式保留骨、承重骨或必要依赖不能强制删除，冲突会停止。
   - **确认可清理骨**（`confirmed_removals`）：审核后逐个指定精确骨名，只授权这些节点，不自动包括后代。若被其他保留骨依赖，仍不能删除。
   - 可输入逗号分隔的精确骨名，也可在源骨架 Pose 模式选骨后点击相应“加入”按钮；之后回 Object 模式重新扫描。
   - 默认保留承重骨的直接叶子节点作为末端候选。这是保守候选，不表示所有叶子均有用；无用末端可以显式确认清理。关闭此选项不意味着允许删除用途未确认的骨。
   - 待审核骨默认保留，可以先带着它们生成工作副本；不要求为了继续流程就强行判断它们无用。切换源衣物会清空旧的保留/清理规则及扫描计划。
4. 在“T pose 骨骼映射”检查左右上臂、前臂、手腕。支持日文名、MMD 日文元数据以及 Blender `.L/.R` 别名；全角英数只用于识别，不改原骨名。重名冲突不自动猜测。
5. 点击“备份并创建姿态候选”。先保存 `before.blend`，再创建独立衣物和骨架副本。自动姿态以源骨架局部 X 为左右方向；如果本模型方向或控制方式不适合，关闭自动摆姿态，手工调整候选。
6. 检查候选的肩、袖口和手掌方向。需要微调时点击“调整候选骨架姿态”；此阶段只能改 Pose，不能改网格、权重或静止骨架。完成后回 Object 模式，勾选姿态确认。
7. 点击“落实 T pose、清理并验证”。通过后生成干净工作对象和隐藏的未清理 T pose 参考；原输入及姿态候选保留。结果副本保存为 `result.blend`，不覆盖用户当前项目文件。后续保存可用“再次保存结果副本”。

候选/结果以局部视图显示，避免与原模型重叠；退出局部视图可返回原场景范围。详细计划、错误和报告可在文本编辑器查看 `MMD_Preprocess_Report.json`。

## 骨骼清理规则

保留集合的种子为：**保护名单命中的已有身体骨（无条件）**、实际正权重骨、明确指定的参考骨和完整链，以及启用的直接末端候选。然后沿父链、MMD 追加变换源、骨约束目标（包含多目标与 pole）、B-Bone 自定义首尾手柄和显示连接引用迭代补全依赖，直到稳定。

- 身体骨保护独立于当前衣物的权重、是否启用自动 T pose、是否保留可选末端。局部衣物不使用腿、手指或脚趾，不构成删骨理由；禁用变形也不解除保护。
- 内置有限名单覆盖躯干/头颈、四肢、手指/脚趾，常见准标准肩 P/C、捩骨及分配骨、D 腿链、足先 EX、腰取消，以及根/中心/全局移动、眼和足部 IK 控制。项目已确认的 `上半身3` 单独列为保护扩展，不宣称它是统一标准。
- 名单在 `mmd_preprocess_rules.py` 的 `PROTECTED_BODY_NAMES` / `PROTECTED_BODY_EXTENSIONS`。同时识别实际骨名和 MMD `name_j`，兼容全角字符、`.L/.R` 及列明的拼写别名，不改名、不合并。多个骨命中同一别名时全部保护，但姿态映射仍须消除歧义。
- 这是**保留名单**，不是身体权重语义分类器；保留骨骼不会增加衣物权重。只保留已有节点，不创建缺失骨骼，不无条件保留它的所有后代。
- 未知命名（例如没有日文元数据的自定义英文名）不强行推断语义：没有其他保留理由、又没有明确清理授权的骨，全部进入 `review_required` 并默认保留。这包括未知身体扩展及导入器辅助骨。通过“额外保留骨”可把已确认的身体扩展标为明确保留。
- 待审核骨加入保留集合后，**再次补全依赖**；若它依赖已请求清理的节点，停止并报告冲突。不能用“没有本衣物权重”“没有命中标准名”自动生成确认清理列表。

- 不把非 Humanoid 或名单外骨骼自动视为动骨。
- 不把零权重骨自动当占位骨：中间骨可以带动承重子链；末端和辅助控制也可能必需。
- 不按 `_dummy_`、`_shadow_` 等前缀批量删除。只有明确确认可清理、且不在保留集合及依赖中的节点才是删除候选。
- 不把同一根骨下的所有分支默认归于同一件衣物。完整链归属不明确时，由用户/Agent 调整链根与排除范围。
- 所有删改发生在独立副本，不破坏共享原骨架的其他使用者。
- 删骨后只清除对应已删骨、无任何顶点成员的空组；不清除低权重、不限制骨数、不重新归一化。
- `mmd_edge_scale`、`mmd_vertex_order` 等非骨骼组不纳入身体预算，也不作为无用权重删除。

## 验证与状态

- 保存和检查源模型、候选结构及配置摘要。源输入、规则或映射变化后必须重新扫描；候选网格/权重/骨架结构变化后禁止继续。已经生成的结果带标记，不能重复烘入。
- 落实静止姿态前后比较求值顶点；保留拓扑、权重、UV、材质分配、锐边和自定义法线。最大法线角差超过 1° 或位置误差超过尺度相关阈值时停止该候选。
- 0.2.18：MMD 腿部 IK 在重设静止后可能重新求解出微小残差。仅对不超过模型尺度 `2e-5` 的残差，支持通过有效 LBS 矩阵求逆补偿静止网格坐标；不修改权重、骨约束或动画控制。要求有效权重已归一化、矩阵可稳定求逆且非分段 B-Bone 蒙皮。补偿后仍须通过原有真实 Blender 求值位置阈值和法线检查；较大变形拒绝补偿。报告 `rebase_roundoff_compensation` 记录补偿前后误差及最大坐标修正，不能将此机制当成大范围骨架修复。
- 在未清理的 T pose 参考与清理结果之间测试代表性控制骨、追加变换源、确认链根和混合动作。比较真实 Blender 求值网格，并检查有有效运动。
- 失败不修改原件；结果阶段的临时副本被丢弃，姿态候选保留用于检查。
- **编辑就绪（EDIT_READY）**：可以开始顶点适配。
- **优化器入口**：必要 pose constraints 仍在时明确显示未满足当前优化器入口。不能以静态烘入 T pose 代替完整动作依赖转换。无约束结果也仍须完成后续目标映射、区域模式等权重流程检查。

动作测试证明的是清理前后在所测状态中的一致性，不宣称任意动作、原 PMX 物理运行时或重设静止前后的全部行为等价。

## 第一版范围

支持单件衣物、单个普通 LBS Armature 修改器及可在副本内重定向的骨约束。多个衣物需要作为整体处理时，暂不逐件清理共享动骨后再盲目合并；应先明确共同资产范围。

以下情况停止并提示专门处理：形态键/SDEF 数据、DQS/Preserve Volume、骨骼封套、修改器遮罩、额外修改器、动画或驱动、对象约束、骨约束引用外部对象、自动摆姿态映射缺失/冲突/层级异常。不会自动删掉这些功能以通过检查。

只复制指定网格与骨架。其他物体形式的饰件、刚体、关节、外部物理设置不属于本模块迁移范围；相关原件保留。已有骨骼名称与权重不会改成 KK 名称。

## 本案例的配置与验证

`003_裙子.001` 本轮手工确认的完整链包括 15 条 `q_0_*` 裙摆链、Tie/Rope/Bag 衣物链和 `胸上2.L/R` 链；`带_0_1.L/R` 分支属于另一件 `004_绳`，因此排除。本配置是案例信息，插件不硬编码这些名称。

0.2.15 旧试验为 423 → 242 骨、删除 151 个空组；它删除了衣物未使用的部分身体骨，此行为已被 0.2.16 的无条件保护纠正，不再作为预期输出。新版为 **423 → 309 骨**，删除 96 个空组，保留 52 个约束；原权重和网格属性不变，26 个测试状态的清理前后位置最大差为 0。测试直接核对受保护集合、权重和动作求值，结果见项目 `plugin-integration-body-protection/integration.json`。旧场景不会在插件升级时被自动修补；需要从保留的原始输入重新生成结果。

0.2.17 增加未知骨默认保留。在这个案例中，仅确认 `带_0_1.L/R` 整条外部资产分支可清理时，其他用途未确认节点继续保留；回归测试随后显式载入这个固定案例的历史清理清单，验证经过明确确认才可复现 309 骨结果。历史清单只用于测试，不是生产环境的自动授权规则。

案例记录见工作区 `garment-workflow/analysis/mmd-to-kk/preprocess-script-design.md`；插件回归测试为 `tests/test_mmd_preprocess_rules.py` 和 `tests/test_mmd_preprocess_blender.py`。

## Agent / 脚本接口

界面和 Agent 使用同一后端，配置为普通字典：

```python
from kk_vrc_cloth_tools import mmd_preprocess as prep

cfg = {
    "auto_tpose": True,
    "preserve_tips": True,
    "pins": [], "chains": [], "excluded": [],
    "confirmed_removals": [],  # 已审核可清理的精确节点；默认无授权。
    # 可省略 mapping 以读取 MMD 候选；缺失或冲突时不能自动摆姿态。
}
scan = prep.scan(source_object, cfg)
job = prep.prepare(source_object, cfg, scan["stamp"], output_directory)
# 用户/Agent 检查候选，必要时只调整候选骨架的 Pose。
result = prep.finalize(job)
prep.save_result(result)
```

`finalize` 会重新核对源指纹、计划及候选结构。Agent 不应把未经归属判断的整棵子树直接配置成完整链，也不能通过修改报告跳过失败检查。

Agent 先查看 `scan['plan']['review_required']`，依据源模型骨架、依赖和资产归属确认用途。不能确定时继续保留，必要时请求人工判断；不能将整个待审核列表机械复制到 `confirmed_removals`。确认身体扩展用 `pins`；确认无用节点用 `confirmed_removals`；整条分支用途已明确才使用 `excluded`。旧版计划会在重新核对时失效，必须重新扫描。

每轮目录保存 `before.blend`、`plan.json`、原权重、`prepared_weights.json`、`prepared_bones.json`、`result.json` 和 `result.blend`。报告包含实际保留原因、删除清单、必要约束数量及动作验证结果。
